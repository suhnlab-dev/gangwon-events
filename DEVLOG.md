# 🛠 개발일지

## 2026-08-26

### 오늘 만든 것

강원도에서 열리는 축제와 행사 정보를 한곳에서 볼 수 있는 「강원 이벤트」 웹앱을 만들었다.

처음에는 강원도 관광 관련 공공데이터를 이용해서 앞으로 열릴 행사를 날짜별로 보여주는 것을 목표로 했다.

### 구현한 기능

- 📅 행사 기간별 조회
- 🗓 2026년 1월부터 2027년 8월까지의 기간 설정
- 🎪 관광공사 행사 데이터 연동
- 🔄 공공데이터 API를 이용한 행사 정보 가져오기
- 🌐 GitHub Actions를 이용한 데이터 자동 업데이트
- 🌎 GitHub Pages를 통한 웹서비스 배포

### 개발하면서 생긴 문제

처음에는 공공데이터 API에서 행사 정보를 가져오는 과정에서 데이터가 정상적으로 나오지 않는 문제가 있었다.

API 요청 시 필수 파라미터가 누락되어
`NO_MANDATORY_REQUEST_PARAMETERS_ERROR`가 발생하기도 했고, 날짜 범위를 지정했는데도 결과가 0건으로 반환되는 경우가 있었다.

또한 API 요청 자체가 시간 초과되어 `TimeoutError`가 발생하기도 했다.

여러 API 요청을 테스트하면서
행사정보조회 API의 실제 요청 방식과 파라미터를 확인했고,
기간을 명시해서 데이터를 요청하도록 수정했다.

행사정보조회 API(`/searchFestival2`)가 어떤 파라미터를 필수로 요구하는지 확인했다.

이후 `eventStartDate`와 `eventEndDate`를 포함해 2026년 1월 1일부터 2027년 8월 26일까지의 기간을 직접 지정하여 API 요청을 테스트했다.

또한 API에서 반환되는 `totalCount`와 실제 `items`의 내용을 확인하면서 요청은 정상적으로 처리되더라도 해당 조건에서 반환되는 행사 데이터가 없을 수 있다는 점도 확인했다.

최종적으로 GitHub Actions에서 실행되는 Python 코드가 조회 시작일과 종료일을 명시해서 API를 요청하도록 수정했고, API 응답을 확인한 뒤 웹페이지에 데이터를 반영하는 방식으로 정리했다.

이후 GitHub Actions에서 Python으로 API를 호출하고 가져온 데이터를 웹페이지에서 사용할 수 있도록 처리했다.

### 추가로 시도한 것

강원관광공사 데이터뿐만 아니라 춘천문화재단, 홍천문화재단 등 다른 기관의 행사 정보도 함께 가져와 하나의 행사 목록으로 만들려고 했다.

하지만 기관별 API 제공 상태와 데이터 형식이 달라 일단 현재 사용할 수 있는 데이터만 연결하는 방향으로 정리했다.

또한 여러 출처의 행사 데이터를 합칠 경우 같은 행사가 중복으로 나타날 수 있기 때문에 중복 행사를 제거하는 방법도 고려했다.

### 오늘 배운 것

- 공공데이터 API 사용 방법
- API 요청에 필요한 파라미터 확인
- 날짜 범위를 이용한 데이터 조회
- Python으로 API 데이터 가져오기
- GitHub Actions를 이용한 자동화
- API 오류와 Timeout 문제 확인하기
- 서로 다른 데이터 출처를 하나로 합칠 때 생기는 문제

### 다음에 해볼 것

- [ ] 다른 강원도 행사 데이터 추가
- [ ] 행사 지역별 필터
- [ ] 행사 종류별 필터
- [ ] 춘천 행사만 보기
- [ ] 행사 상세 페이지 추가
- [ ] 중복 행사 자동 제거 개선

---

## 💭 메모

처음에는 단순히 강원도에서 열리는 행사를 한곳에 모아서 보여주면 될 것이라고 생각했다. 
요즘 스스로 '가까운 곳에 열리는 행사 없나?' 하는 생각을 주말에 많이 하곤 해서.. 검색하면 너무 여러 기관에서 각자의 이벤트만 올리는것을 보고
사용자가 한번에 통합해서 정보를 얻으면 좋겠다는 생각에 시작했다.

그런데 실제로 웹앱을 만들기 위해 데이터를 찾아보니 생각보다 상황이 복잡했다. 복잡하다기보다 통합되어 있지 않았고, 홍보도 되어있지 않았다.
특히 지역 기관의 경우 API가 제공되지 않거나, 예전에 만들어진(2023년) API가 현재는 정상적으로 작동하지 않는 경우도 있었다.

공공데이터포털에서 제공하는 데이터 외에 지역별 문화재단이나 관광 관련api를 찾으니 별도의 사이트에서 정보를 제공하고 있었다.
아직 빈공간이 많다. 내 수준과 별개로 어떤 한 서비스를 밀도있게 구현하기 위해 필요한 정보는 부족하다고 느껴진다.

그래도 직접 API를 테스트하고 오류를 확인하면서 실제로 데이터를 가져와 웹페이지에 표시하는 것까지 구현했다.

그래서 모든 지역의 행사 데이터를 한 번에 통합하는 것보다는 현재 실제로 사용할 수 있는 데이터를 우선 연결하고,
나중에 추가로 활용할 수 있는 데이터가 생기면 확장하는 방향으로 프로젝트를 우선 마무리 해봤다.

생각보다 데이터를 구하는 것 자체가 개발 과정에서 큰 부분을 차지한다는 것을 알게 됐다.

작은 아이디어라도 실제 데이터를 연결해서 서비스로 만들어보는 과정을 경험할 수 있었다.
